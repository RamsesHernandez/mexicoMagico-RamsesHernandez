import cartWidget from '../images/cartWidget.png'

const CartWidget = () => {
    return(
        <div>
            <img src={cartWidget} alt="carrito"></img>
        </div>
    )
}

export default CartWidget